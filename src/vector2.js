/**
 * Adapt and extend the Vector2 class as needed to complete the excersise.
 */
class Vector2 {

    constructor(x = 0, y = 0) {
        this.x = x;
        this.y = y;
    }
}

module.exports = Vector2;